// stud
fn import(string: String) RymModule => {
	// TODO
}

const io = import("std.io");
const ParseError = example_package.Error;

// use std.io.{self, IoError};
// use package.Error as OtherError;
// use package.prelude.*;

/// Combined error type
enum Error {
	Io(io.Error),
	Parse(ParseError),
	Other { message: String },
    Unkown,
}

fn main() Result((), Error) => {
	// TODO read cli arguments
};
