func fib_recurse(n) {
	if n == 0 || n == 1 {
		return n
	}

	fib_recurse(n - 1) + fib_recurse(n - 2)
}

const N = 30 // higher values take way too long to compute
print("fib_recurse("+ N + ") => ")
println(fib_recurse(N))
