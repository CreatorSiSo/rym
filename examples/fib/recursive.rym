fn fib_recurse(n) {
	if n == 0 || n == 1 {
		return n
	}

	fib_recurse(n - 1) + fib_recurse(n - 2)
}

const N = 78 // 78 is max for result without deviation
print("fib_recurse("+ N + ") => ")
println(fib_recurse(N))
