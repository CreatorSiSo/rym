func main() -> Result<(), Error>, !Io {
	const msg = "Hello World"
	print(msg)

	mut num = 2 / 4 * (10 - 1)
	// TODO Do not insert space between items if leeding or trailing whitespace is detected
	print("Number:\t", num)

	const msg = msg + "!"
	print("Combined:\t", msg, num)

	might_return_error()?
	const maybe_value = do_stuff(testing).even_more(false)

	Ok(())
}
