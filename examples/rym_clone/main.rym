use std.io.{self, IoError};
use package.Error as OtherError;
use package.prelude.*;

/// Combined error type
const Error = IoError | OtherError;

const main = fn() Result<(), Error> => {
	// TODO read cli arguments
};
